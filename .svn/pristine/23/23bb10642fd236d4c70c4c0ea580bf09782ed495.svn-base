<!--pages/financial/note/index.wxml-->
<block wx:for="{{items}}" wx:key="">
    <w-note-item
            bind:tap="{{item.fn}}"
            name="{{item.name}}"
            tip="{{item.tip}}"
            num=""
            src="{{item.src}}"
            isNew="{{item.isNew}}"
    />
</block>


